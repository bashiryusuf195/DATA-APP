# Android build environment variables.
# Loaded automatically by: npm run android:build (vite build --mode android)
# Capacitor WebView loads the app from https://localhost, so relative API paths
# fail. This file sets the absolute production URL so all fetch calls resolve.

VITE_API_BASE_URL=https://api.hivedata.ng
